## Michael V. Sweeney — Personal Website

Static, single-page site (HTML/CSS/JS). No build step required.

### Run locally

- **Option A (quickest)**: open `index.html` in your browser.
- **Option B (recommended)**: start a tiny local server (helps with caching + consistency)

```bash
cd "/Users/numbaonesween/michael-sweeney-site"
python3 -m http.server 5173
```

Then open `http://localhost:5173`.

### Customize

- **Content**: edit `index.html`
- **Design**: edit `styles.css`
- **Behavior** (dark mode, mobile nav, contact form): edit `script.js`

